# version: '3.9'

services:
  # deserialize-db:
  #   image: postgres:15
  #   restart: always
  #   container_name: deserialize-db
  #   volumes:
  #     - deserialize-pgdata:/var/lib/postgresql/data
  #   ports:
  #     - "5480:5432"
  #   environment:
  #     POSTGRES_USER: ${POSTGRES_USER}
  #     POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
  #     POSTGRES_DB: ${POSTGRES_DB}

  redis:
    image: redis:alpine
    restart: always
    container_name: deserialize-evm-redis
    command: sh -c "redis-server --requirepass ${REDIS_PASSWORD} --appendonly yes"
    environment:
      - REDIS_PASSWORD=${REDIS_PASSWORD}
      - REDIS_PORT=${REDIS_PORT}
    ports:
      - "${REDIS_PORT}:6379"
    volumes:
      - redis-data:/data
    #   - ./redis.conf:/usr/local/etc/redis/redis.conf
    # command: redis-server /usr/local/etc/redis/redis.conf

volumes:
  # deserialize-pgdata:
  redis-data:
